# Web & Style — állapot

_Utoljára frissítve: 2026-07-28 (Sketchfab-város végig bent marad Act4-ig + nappal/éjszaka vált vele együtt)_

Élő munkanapló: mit csinálunk, merre haladunk, mi a terv. Minden nagyobb lépés után frissül.

---

## 📍 Hol tartunk most (2026-07-25, singularity-session után)

**MEGÉPÜLT a vihar utáni teljes payoff-lánc + a navigációs handoff** (a fő narratív ív most záródik):
a piros pirula → vihar (megvolt) → **minden a helyén lilás KÓDDÁ válik** (kéz/szobor/növények/talaj/felirat) → **szétrobban** → **spirál-örvény** (information-singularity, shader-reprodukció) beszippantja a képet → **feketelyuk kitölti a képernyőt** → fekete → **`window.location` átirányítás a webandstyle.com-ra**. Új fájl: `Singularity.jsx`. Tunolható `?arrival` "Szingularitás" szekció (időzítés-csúszkák + "szekvencia előnézet" scrub + a Vihar "Teszt indítás" a teljes láncot lejátssza, editorban navigáció nélkül). Screenshottal végig igazolva (vihar→kód→örvény→fekete).

Korábbi kész blokkok (részletek a "Kész" szekcióban):
1. **A cső lecserélve CHROMATIC JOURNEY-re** (`ChromaJourney.jsx`) — kamera a Sketchfab szivárvány-donut BELSEJÉBEN, roll-lal. Look beégetve (env 0.2 / exp 0.92 / bloom 0.30 / küszöb 0.56 / CA 0.0058). **2026-07-25: OPTIMALIZÁLVA** (gltf-transform: weld + 0.6 simplify + webp + quantization) → **18,4MB → 6,7MB**, look változatlan. Labor: `?chroma`.
2. **A piros pirula → VIHAR/földrengés** (`GardenSky` + `StormShake` + pillangó-menekülés) — MEGÉPÍTVE, tunolható (`?arrival`).

**Következő lehetséges lépések:** (a) CC-BY-4.0 attribúciónak production-hely kell (jelenleg csak a `?chroma` panelen — a leendő footer/`#agency`-be); (b) `#agency` szekció; (c) élesben végigtesztelni a valódi scroll-folyamban a pirula→redirect láncot; (d) a `information-singularity.zip` (158MB, gyökér) törölhető — a look shaderként reprodukálva, az asset nem kell. Lásd a "Terv" szekciót.

---

## 🎯 Most éppen ezen dolgozunk

**KONCEPCIÓVÁLTÁS (2026-07-23): a NYUSZI TELJESEN KIVÉVE, helyette VIHAR/FÖLDRENGÉS.** A user leállította a nyuszi-irányt is (a rig/animáció nem volt elég jó, túl kötött), és egy sokkal erősebb Matrix-pillanatot kért a piros pirulára: **földrengés**. A `Rabbit.jsx`, `RabbitLab.jsx`, a `?bunny` labor, a `RabbitMarkers`, az összes `rb*/head*/ear*/nose*/emergeDur` cfg-mező, a `rabbit.glb` + `eyeball-test.glb` — MIND TÖRÖLVE. A `rabbitTriggerRef` → `stormRef` lett.

### A VIHAR (2026-07-23) — MEGÉPÍTVE, build/lint hibátlan, screenshottal igazolva

A piros pirulára kattintva (vagy `?arrival` "Teszt indítás" gomb) elindul a vihar, minden a **GardenSky shaderébe** építve (nem külön overlay — user döntés), plusz a `StormShake` kamera + a pillangók menekülése:

1. **Az ég sötét lilába borul**, a felhők elsötétednek, bruised-violet lit-élekkel (`uStorm` 0→1, `stDur` alatt épül fel smoothsteppel; a szivárvány kifade-el).
2. **Sárga villámlás**: sztochasztikus felhő-flash + jagged bolt (fő + egy elágazás), `lightningFlash()` in-shader, `uLightning`/`stLight` szabályozza. Temporális — stillen nem mindig látszik, de a logika megvan.
3. **A pillangók elmenekülnek**: `stormRef`-re a `flee` 0→1 ~1mp alatt, a `PATHS` sodródást felülírva felfelé + a legközelebbi képernyőszél felé repülnek, pánik-jitterrel, és nem csapkodják tovább a falat.
4. **A kamera megremeg** (`StormShake.jsx`): kicsi, FŐLEG ROTÁCIÓS jitter helyben (soha nem dolly/utazás — az felfedné a lapos kert-síkot, ld. lecke lent) + kezdeti lökés (`onset`), `stShake` szabályozza. reduceMotion alatt kikapcsol.
5. **A LED-falon hullámszerűen kiélesedik a zöld kód**: `uStormWave` egy táguló front a képernyő közepéből; a kód a front MÖGÖTT válik sűrűvé/olvashatóvá (a `column`/`alive` küszöbök lejjebb tolva), a fronton EXTRA flare. `stWave` szabályozza a söprés idejét. Az árva `uMegaAmp`-mechanikát (tear/posterize/slip) újrahasznosítjuk: `max(uMegaAmp, uStorm)` hajtja.

**cfg-mezők** (`?arrival` "Vihar" szekció, 4 csúszka + Teszt indítás/Visszaállítás): `stDur` (felépülés mp), `stWave` (kód-hullám mp), `stShake` (rengés erő), `stLight` (villám erő). A `stormRef = { triggered, startedAt }` — a `startedAt`-et a GardenSky useFrame bélyegzi az első triggered-frame-en, ezt olvassa a StormShake és a Butterflies is (közös időalap).

**Screenshot-igazolás (playwright-core + cached chromium, ?arrival + Teszt indítás gomb):** nyugodt → onset (kék ég + szivárvány + pillangó még megvan) → csúcs/full (teljes zöld kód-fal sötét lila háttéren, pillangók eltűntek, kert-előtér érintetlen). A kompozíció tökéletes Matrix-reveal: az előtér (ösvény/növények/kéz+pirula) a "valóság", a LED-fal-ég mögötte kódba tör.

### A SZINGULARITÁS + NAVIGÁCIÓS HANDOFF (2026-07-25) — MEGÉPÍTVE, build/lint hibátlan, screenshottal igazolva

A vihar csúcsa után a jelenet nem áll meg — beindul a teljes payoff-lánc (user-vízió: „minden a helyén kóddá válik, mintha eleve kód formálta volna, majd szétrobban egy örvénybe/feketelyukba, aztán fekete → webandstyle.com"). Új fájl: **`Singularity.jsx`**, egyetlen közös óra (`singularityState(stAge, cfg)`), amit minden réteg olvas (a `stormRef.startedAt`-hez horgonyozva). Fázisok (cfg-mezők): `stHold` (vihar kitartása) → `sgCode` (kóddá válás) → `sgBurst` (szétrobbanás+örvény) → `sgCollapse` (feketelyuk-összeomlás) → `sgBlack` (feketeség) → redirect.

1. **Kóddá válás a helyén (lilás Matrix-kód):** minden objektum anyagába `onBeforeCompile`-lal injektált kód-dissolve fragment-chunk (`CODE_DISSOLVE_PARS/FRAG`, `patchCodeDissolve`): a felület `uCode` 0→1 alatt lilás glyph-mezővé válik (a sziluett olvasható marad — „a kéz/szobor kódból van"), majd `uBurst` alatt zaj-küszöbös `discard`-dal széterodálódik. Bekötve: **kéz** (`PillHand`, `patchCodeDissolve`), **szobor+növények** (`GardenModel` — a meglévő szél-bend onBeforeCompile-jába fésülve, a nem-bendelő szoborra is), **talaj/környezet** (`GardenBg` custom shaderébe fűzve), **felirat** kifade-el (`Title3D` a `uCode`-dal). Az ég a vihar zöld kódját hozza — együtt: a teljes kép kód.
2. **Örvény = a KÓD MAGA folyik a lefolyóba (2026-07-25 finomítás, user-kérés):** a user visszajelzése szerint az első verzió (különálló pötty-spirál ScreenQuad) nem érződött beszívónak és nem volt középen. **Javítva:** (a) **a `GardenSky` kód-shaderébe drain-warp** került (`uDrain`) — a kód UV-ját a középpont felé csavarja + befelé mintavételez (folyamatos, időalapú pörgéssel), így MAGA a zöld Matrix-kód örvényleve belefolyik a központi „lefolyóba", mint víz a plughole-ba; a `SingularityVortex` ScreenQuad már csak a fekete lyukat + izzó cián/magenta peremet + halvány beszívó fényt adja (a saját pötty-spirálja visszavéve). A drain a **TELJES képernyőt** lefedi (az envelope sose 0 a széleken, a sarkok is beörvénylenek — `mix(0.55, 1.0, ...)`), és a kód a szingularitás alatt **teljes RGB kromatikus** (zöld → szivárvány `hue()` a drain-szög + sugár szerint), nem csak zöld. (b) **Középre igazítás:** a `gl_FragCoord/uRes` a devicePixelRatio miatt elcsúszott (a lyuk ~0.40-nél volt) → `vScreen` varying (`position*0.5+0.5`) a vertex shaderben, dpr-független, most dead-center. A nyers `information-singularity.zip` asset (2,36M tri + Shutterstock-vízjel, headless Blenderrel vizsgálva) nem éles-képes; a look shaderként él tovább (nulla teher, nincs licenc-gond).
3. **Handoff:** a lánc végén (`st.done`) a `SingularityVortex` `onDone`-t hív → `ArrivalScene` `onSingularityDone` → **`window.location.href = RED_PILL_URL`** (webandstyle.com). Csak élesben (frozen alatt tiltva, hogy az editor-teszt ne navigáljon), egyszer (`redirectedRef` + `doneRef`). A fekete képernyő elfedi a betöltést. reduceMotion alatt a spin/shake/villám kikapcsol, de a lánc lefut és átirányít.

**cfg-mezők** (`?arrival` "Szingularitás" szekció): `stHold`/`sgCode`/`sgBurst`/`sgCollapse`/`sgBlack` (mp), + "szekvencia előnézet" pipa (`sgPreview`) és `sgAt` scrub a teljes szakasz végigtolásához. A Vihar "Teszt indítás" gomb a teljes láncot lejátssza.

**Screenshot-igazolás:** preview-scrub (vortex spirál → fényes-peremű feketelyuk → fekete) + valós idejű "Teszt indítás" folyam (~3,6mp: teljes kód-kép + formálódó örvény; ~4,4mp: hős-pillanat — magenta/cián spirál a zöld kódmezőn; ~5mp: fekete). A kompozíció award-szintű Matrix-payoff.

---

### (ARCHIVÁLVA) A korábbi nyuszi-koncepció — 2026-07-21, TELJESEN ELTÁVOLÍTVA 2026-07-23

<details>
<summary>A nyuszi "beosonás + fejfordulás" jelenet (kivéve, itt marad referenciának a kamera-architektúra tanulság miatt)</summary>

A user leállította a korábbi "5-lépéses hop-storyboard → LED-fal becsapódás → navigáció" irányt, és egy teljesen új jelenetet kért helyette:

1. A nyuszi csak részben bukkan elő a növényzet mögül (nincs ugrálás, nincs helyváltoztatás).
2. Finom mozdulatok: enyhe fejbillentés, egy-egy fülrezdülés (aszimmetrikus, késleltetett), alig észrevehető orr-mozgás.
3. A nyuszi a kamerába néz, majd megmerevedik.
4. *(még nem épült meg)* A kamera gyorsuló dolly-in mozgással közelít a sötét szeméhez.
5. *(még nem épült meg)* Mielőtt a kamera belevágna a geometriába, a szem fullscreen glitch/portál-átmenetté válik, ami megnyitja a mind-jungle-t.

**A HOP-RENDSZER TELJES EGÉSZÉBEN ELTÁVOLÍTVA** (`Rabbit.jsx`, `RabbitLab.jsx`, `ArrivalScene.jsx`-ből egyaránt) — nincs `waypointsFromCfg`/`rabbitPose`/`segDurationsFromCfg`/`rotationsFromCfg`/`doneTime`/`impactTime`, nincs 5 lépés-pont, nincs `r2*/r3*/r4*/ri*/rt1-4/rr2-5` cfg-mező, nincs lépés-fagyasztó gomb, nincs LED-fal-becsapódás/`onDone`/navigáció. Csak EGY hely maradt (`rbx/rby/rbz` + `rr1` irány + `rscale`), és a fej/nyak/fül/orr csontmozgás.

### Technikai audit (mielőtt bármi épült volna) — GLB-szintű, read-only vizsgálat

A `rabbit.glb` **egyetlen SkinnedMesh** (5208 vertex, ~2578 háromszög), **47 csontos Armature** (Head_M/Neck_M/Ear_L,R/Nose_M mind ÖNÁLLÓ csont-node — ezért lehet fejet/fület/orrot procedurálisan forgatni Blender nélkül), **1 baked animáció** (0,43mp-es helyben-ugrás hurok, ma már NEM használt), **NINCS morph target/shape key**, **1 anyag/1 textúra (256×256!)** a TELJES testre — a szem csak festett 2D-folt, nincs saját geometriája. **Kategória: B** (részben alkalmas — a peek+fejfordulás Blender nélkül, kódból megoldható; a meggyőző szem-KÖZELKÉP viszont valódi szem-geometriát és sokkal nagyobb felbontású helyi textúrát igényelne, mert a jelenlegi 256×256-os, egész testre osztott textúra garantáltan pixeles/életlen lenne egy zoom alatt).

### PoC-fázis a `?bunny` laborban (2026-07-20-21) — véglegesítve

- **Fehér szőr, textúra-detail megtartva:** `whitenTexture()` (`Rabbit.jsx`) — a sötét pixeleket (szem pupillája) érintetlenül hagyja, mindent mást deszaturál+fehérít. **Anyag-átlátszóság hiba javítva:** a forrás GLB `alphaMode:"BLEND"` + per-vertex alfa miatt a nyuszi az átlátszó render-sorba került a (szintén blend-elt) fűvel együtt, rendezetlen rajzolási sorrenddel → fűszálak látszottak át rajta. Fix: `transparent:false, depthWrite:true, alphaTest:0` mindenhol.
- **Letöltött szemgolyó-asset megvizsgálva:** `eyeball-with-brown-iris.zip` (Blender-forrás, 2 mesh: `Sphere` iris/pupilla 2048×2048 textúrával + `Roundcube` cornea-dudor, ~5250 tri összesen) — read-only Blender-headless inspekcióval ellenőrizve, majd EXPORTÁLVA → **`public/assets/models/eyeball-test.glb`** (a forrás `.blend` sosem lett módosítva/mentve). **Jelenleg NEM használt sehol** — a user szerint ekkora méretben (`eyeScale~0.02`) nem sikerült ügyesen ráilleszteni/elforgatni a festett szemre, a sima procedurális fehér gömb jobban nézett ki. Megmarad jövőbeli felhasználásra (ld. "Következő lépés" lent).
- **Kamera-architektúra tanulság (fontos, ha újra elővesszük a szem-zoomot):** egy korábbi PoC-verzió a kamerát a nyuszi pozíciójához képest számolta + OrbitControls-t engedett a laborban — ez FELFEDTE a kert-háttér 3D-síkját oldalról (torz "tapéta" lett), mert a háttér EGY FIX kamerához komponált 2D kép egy 3D síkon. **Megoldás, ami bevált:** a labor háttere mostantól sima CSS/DOM `background-image` egy átlátszó Canvas mögött (NEM 3D-sík), a kamera pedig FIX, pontosan az `ArrivalScene` alap-kameráját másolja (`position:[0,0,5], fov:42`) — sosem mozog, sosem forog. Ha legközelebb a dolly-t építjük, ez KÜLÖN, "Transition preview" módban kell hogy történjen, neutrális/fekete háttérrel — NE ugyanabban a nézetben, ahol a kert-kompozíciót szerkesztjük.
- **Labor↔production koordináta-eltérés (talált és javított hiba):** a user élőben letesztelte a fejfordulást az `?arrival`-ban, de a nyuszi nem látszott. Ok: a Step1 Z-mélysége (akkor -7.3) MÖGÉ esett a kert-háttér síkjának (bz:-6.2) — a kamerához közelebbi (opaque-nak számító) háttér egyszerűen eltakarta. A `?bunny` lab CSS-háttere ezt nem tudta megmutatni (annak nincs 3D mélysége, bármi "jónak tűnik" ott). **Tanulság:** a `?bunny` lab jó a MOZGÁS/PÓZ megítélésére, de a végső MÉLYSÉG-kalibrációt mindig az `?arrival`-ban, a valódi 3D háttérrel szemben kell ellenőrizni.
- **Fejek/fülek/orr mozgás-modell (`applyPeekBonePose`, `Rabbit.jsx`, megosztva a laborral):** minden csontnál a BIND-kvaternionhoz képest additív forgatás — Head_M (pitch/yaw/tilt), Neck_M (enyhe fordulás), Ear_L (gyors, aszimmetrikus "flick"), Ear_R (kisebb, ~180ms késleltetett flick), Nose_M (2 apró twitch). 0,6mp-es "settle" beállás, utána VÉGLEGESEN megáll (nincs loop, nincs remegés) — `PEEK_TIMELINE_DURATION=1.4mp`.
- **Egységesítve (2026-07-21):** a fej/fül/orr-mozgás paraméterei (`headYaw/headPitch/headTilt/neckTurn/earLAmp/earRAmp/noseAmp/playSpeed`) most KÖZVETLENÜL a megosztott `?arrival` config (`DEFAULT_CFG`) részei — nincs többé külön `ws-bunny-peek` localStorage-kulcs, a `?bunny` lab ugyanazt a `ws-arrival` configot olvassa/írja, mint a production.

### Jelenlegi DEFAULT_CFG nyuszi-mezők (2026-07-21, a user HARMADIK élő kalibrálása után)

Végső hely (ahol megáll): `rbx:0, rby:-4.9, rbz:-5.5`, `rr1:-46` (irány), `rscale:1.46`, `rgy:-0.44`. Rejtett kezdőpont (a "P" mögött, MÉG NINCS élőben kalibrálva, csak becslés): `rbx0:0.6, rby0:-2.3, rbz0:-5.6`. Fej/fül/orr: `headYaw:22, headPitch:11.5, headTilt:-9.5, neckTurn:8.5, earLAmp:12, earRAmp:6, noseAmp:0.9, playSpeed:0.5`.

### Az "előbukkanás" mozdulat HÁROMSZOR iterálva (2026-07-21, ugyanaznap)

**1. próbálkozás (elvetve):** "felbukkanás alulról" — a nyuszi helyben, Y-ban emelkedett fel + 0-ról nőtt a mérete. A user visszajelzése: nem ezt akarja — **egyetlen valódi UGRÁS legyen a "P" mögötti rejtett pontból erre a végső helyre**, nem egy helyben lebegő felbukkanás.

**2. próbálkozás (elvetve, "ez az ugrás béna"):** külön "rejtett kezdőpont" (`rbx0/rby0/rbz0`) + "végső hely" (`rbx/rby/rbz`), a kettő közt `Math.sin(t*π)`-bounce-os ÍVELT ugrás (`emergeHopHeight` szabályozta az ívmagasságot). Útközben egy KÜLÖN hiba is előkerült: a "Teszt indítás" gomb csak EGYSZER indította a mozgást — ha a nyuszi már landolt, a gomb újbóli megnyomása (pl. csúszka-állítás után) semmit nem csinált, mert a `Rabbit.jsx` csak `!triggered` állapotból nullázza az időzítést. **Javítva** (`ArrivalScene.jsx`): a gomb mostantól mindig előbb `{triggered:false}`-ra állít, majd egy `requestAnimationFrame`-mel újra `{triggered:true}`-ra — így minden kattintás garantáltan friss lejátszást ad.

**3. próbálkozás (jelenlegi, megépítve):** a user szerint az ívelt ugrás helyett **sima, átlós "beosonás"** kell ("mintha odaosonna") — az ív/bounce tag KIVÉVE a kódból, most egy egyszerű smoothstep-elt LERP viszi a nyuszit a rejtett kezdőpontból a végső helyre (`emergeHopHeight` mező is törölve, nincs többé ívmagasság-csúszka). **A 2-pontos jelölő-vizualizáció visszakerült** (`RabbitMarkers`, `ArrivalScene.jsx`): "1" = rejtett kezdőpont (narancs), "2" = végső hely (zöld), fehér összekötő vonal — ugyanaz a mintázat, mint a régi 5-pontos hop-jelölőknél, csak most 2 pontra.

**FONTOS, még hátravan:** a rejtett kezdőpont (`rbx0/rby0/rbz0`) értékei jelenleg csak egy DURVA BECSLÉS (nincs élőben leellenőrizve, hogy tényleg a "P" mögött van-e és tényleg el van-e takarva) — ezt a usernek élőben kell kalibrálnia az `?arrival` "Rejtett kezdőpont" csúszkáival (vagy a `?bunny` labor ugyanilyen szekciójával), a most visszakapcsolt jelölők segítségével, pont úgy, ahogy a korábbi Step1-pozíciókat is kalibráltuk.

Csúszkák: `?arrival` "Nyuszi" szekció alszekciókra bontva (végső hely / rejtett kezdőpont / beosonás időtartama) + `?bunny`-ban ugyanez.

### Következő lépés (ELAKADVA ITT, várja a user vizuális visszajelzését)

A fenti beosonás-mozdulat MÉG NINCS élőben visszaigazolva. **Következő lépés:** a user nyissa meg `?arrival`-t, a most visszakapcsolt "1"/"2" jelölők + összekötő vonal segítségével kalibrálja élőben a "Rejtett kezdőpont" csúszkákat (hogy tényleg a "P" mögött induljon, eltakarva), majd nézze meg a beosonás+fejfordulás EGYÜTTES hatását, hangolja az időtartam-csúszkával ha kell. **Ezután, csak ha ez rendben van:** jöhet a kamera-dolly a szemhez + fullscreen glitch/portál-átmenet — EZ MÉG EGYÁLTALÁN NINCS MEGÉPÍTVE production kódban (a korábbi PoC-verzió eye-anchor/dolly/handoff kísérlete teljesen el lett távolítva a kamera-architektúra hiba miatt, ld. fent). Ha ide jutunk, alighanem egy ÚJ, KÜLÖN "Transition preview" labor-mód lesz a jó kiindulás (fekete/neutrális háttérrel, ahogy fentebb is szerepel), NEM a meglévő kert-composite nézetben.

**Egyéb nyitott szál:** a `RED_PILL_URL` (`https://cortex-deploy-silk.vercel.app/`) jelenleg SEHOL nincs meghívva (unused-var lint figyelmeztetés is jelzi) — a hop régi `onDone`→navigáció lánca eltávolítva, az ÚJ navigációt majd a leendő dolly+handoff fogja hívni. Addig a piros pirulára kattintás csak a fejfordulást indítja el, sehova nem navigál.

</details>

---

## 🧹 Projekt-takarítás (2026-07-25)

A user kérésére végignéztem az egész projektet és töröltem a nem használt / feleslegessé vált fájlokat (61 követett fájl + a gyökér `chromatic_journey.glb` dup; ~480MB lemezen). **Build + lint utána hibátlan.** Amit töröltem:
- **Holt `src` komponensek** (sehol nem importálva): `Hero.jsx/.css`, `Header.jsx/.css`, `WhatSection.jsx/.css` (régi scaffolding, sosem renderelt), `TubeRide.jsx` (a chroma váltotta le), `experience/TunnelScene.jsx` (elvetett korai cső-iteráció).
- **Holt public asset**: `public/assets/hero/` (hero-loop.mp4 + hero-poster.jpg — csak a törölt `Hero.jsx` használta; a `hero-sprite/hero-poster.jpg` MEGMARADT, azt az `Experience` reduced-motion posztereként használja).
- **Asset-forrás cruft a gyökérben** (mind a `public/`-ba konvertálva, kódból nem hivatkozva): kicsomagolt mappák (`grass/`, `grass (1)/`, `rabbit-rigged/`, `hand-gesture-3/`, `first-person-hands-rigged/`), forrás-zipek (rhododendron, butterflies, statue, flower, garden-vegetation, grass, hand-gesture, jelvion, metropolis, milker, rabbit-rigged, mcp), forrás-glb-k (`Meshy_*`, `hand.glb` 22MB→az optimalizált a public-ban, `rabbit.glb`), junk (`Untitled.txt`, `mcp-1.0.0.zip`).
- **MEGTARTVA**: `CLAUDE.md`, `status.md`, minden élő `src`/`public` asset, a `Heathergreen/Moonscape` fontok (van rájuk bekötés-TODO), és a **logó-forrás PNG-k a gyökérben** (`web&style.png`, `webandstyle logo.png`, `webandstyleonly.png`, 2× ChatGPT-upscayl) — brand-originálok, ezeket szándékosan hagytam (ha kellenek, törölhetők).
- **Megjegyzés:** a törölt fájlok git-KÖVETETTEK voltak, tehát a git-történetben megmaradnak (visszaállíthatók); a working tree-t takarítottam, history-t NEM írtam át. A `.git` mappa mérete emiatt nem csökken, amíg nincs history-rewrite (külön, explicit lépés lenne).

---

## 🚀 Deploy / infrastruktúra (2026-07-18–19)

- **"Coming soon" gate a mind-jungle-en — ÉLESBEN (2026-07-23):** mivel a webandstyle.com domain rákerült a mind-jungle Vercel-deployra, de a valódi Web & Style portfólió (ez a repo) még nincs kész/élesben, a `C:\Users\hibyr\portf\creative-cortex-refactored\index.html` + `styles.css` most egy "coming soon" képernyő mögé van zárva — alapból SENKI nem jut túl a `#coming-soon` overlay-en ("the website and portfolio coming soon"), a nehéz assetek (Three.js/GSAP/`main.js`/chambers) egyáltalán nem is töltődnek be. Bypass teszteléshez: `?preview=alex` a query-stringben (ez adja hozzá a `ws-live` classt a body-ra, és dinamikusan befűzi a scripteket). **DEPLOYOLVA** (`vercel --prod` a `web-and-style-by-alex-s-projects/cortex-deploy` projektbe, egy 562MB-os staging-másolatból, ugyanaz a fájlkészlet mint korábban: `index.html`+`styles.css`+`js/`+`vendor/`+`assets/`) — élesben ellenőrizve mind a `https://www.webandstyle.com`, mind a `https://cortex-deploy-silk.vercel.app` alias-on, mindkettő a "coming soon"-t szolgálja ki.
- **KÉSZ — a mind-jungle (Creative Cortex) élesben fut Vercelen.** A `creative-cortex-refactored` forrás 9,2GB volt (backupok, zipek, `.blend` fájlok, egy `ABOUT ME/` mappa) verziókövetés nélkül — az `index.html` + JS tényleges hivatkozásait végigkövetve kiderült: csak `index.html` + `styles.css` + `js/` + `vendor/` + `assets/` kell (562MB). Ez a tiszta halmaz külön staging mappából lett feltöltve (`vercel --prod`). **Éles URL: `https://cortex-deploy-silk.vercel.app/`** — ez a `RED_PILL_URL` az `ArrivalScene.jsx`-ben (a korábbi, sosem futó `localhost:5173` helyett).
- **KÉSZ — `openai/codex-plugin-cc` plugin telepítve** (`/plugin marketplace add` + `/plugin install codex@openai-codex`, `~/.claude/settings.json`-ban `enabledPlugins`), a Codex CLI is telepítve és bejelentkezve (`codex-cli 0.144.5`). **Ismert korlát:** a plugin háttér-feladatai (`codex:codex-rescue` → háttér Codex Task) ezen a gépen **nem érik el a helyi fájlrendszert/parancssort** — a `codex-windows-sandbox-setup.exe` sandbox-helper fizikailag megvan a lemezen, de a háttér-feladat valamiért nem találja (`orchestrator_helper_launch_failed`), ezért minden ilyen delegálás (Blender-export ÉS sima kódszerkesztés is) elakad. Workaround: ilyen munkát egyelőre közvetlenül, magunk végzünk (Bash/Edit), nem delegáljuk háttér-Codex-feladatnak.
- **Blender-MCP működik, de csak friss munkamenetben csatlakozik.** A `~/.claude/mcp.json`-ban be van állítva (`uvx blender-mcp`), és közvetlen teszttel (`uvx blender-mcp` futtatás Bash-ből) igazoltan működik (Blender fut, az addon a 9876-os porton hallgat) — de EBBE a Claude Code munkamenetbe csak munkamenet-újraindítás után töltődik be. A rabbit.glb exportot végül NEM ezen keresztül, hanem közvetlen headless Blender-hívással (`blender --background --python script.py`) csináltuk, mert egyszerűbb és megbízhatóbb volt egy egyszeri konverzióhoz.
- **playwright-core screenshot-ellenőrzés megbízhatatlanná vált egy hosszú munkamenet végén (2026-07-19–20 este).** Sok egymást követő fájlszerkesztés/HMR-újratöltés + tucatnyi headless Chromium-indítás után a teljes `?arrival` jelenet (18 kert-modell + fontok) cold-load ideje ~15-20mp-ről 40-50mp fölé nőtt, ÉS a `page.screenshot()` GPU-readback gyakran (nem mindig) 15-25mp-es timeout-tal elhasal ("GPU stall due to ReadPixels"). **Workaround, ami bevált:** ha csak egy adott ASSET-et (pl. a nyuszi textúráját) kell ellenőrizni, ne a teljes nehéz jelenetet töltsd be — nyiss egy minimál oldalt, tölts be csak azt a modellt közvetlenül (`GLTFLoader` esm.sh-ról, ld. korábbi statue-konverzió mintája), és a canvas-eredményt `toDataURL()`-lel mentsd ki — ez másodpercek alatt lefut, függetlenül a teljes jelenet lassúságától/GPU-stalljaitól. Ha a teljes jelenetet MINDENKÉPP látni kell, számíts rá, hogy egy friss munkamenetben (dev szerver újraindítás után) gyorsabb lesz, mint egy már sokat szerkesztett/HMR-elt munkamenet végén.

---

## 🏗️ FOLYAMATBAN: a KÉK PIRULA újratervezése — 3D ÉPÜLET-ÉLMÉNY (2026-07-26)

A user új, sokkal erősebb koncepciót kért a `#agency`-re: **egy filmes, görgetés-vezérelt 3D brooklyni épület** (warm „vanilla sky", 20th-Century-Fox hangulat), amire a teljes stúdió-story rá van képezve. Ez LECSERÉLI a jelenlegi DNS/kártyás agency-oldalt. **5 felvonás:**
1. **Landolás (tető):** helikopteres ereszkedés felülről → masthead + az épület felett a „WEB & STYLE — web experience studio" tábla.
2. **Bal / nappal / szolgáltatások:** földszinttől felfelé, minden emeleten egy „What I build" fact (8 db).
3. **Jobb / éjszaka / folyamat:** ég éjszakába vált, esti fények; 5 véletlen ablak felgyullad + kamera odaközelít, 5 folyamat-fact.
4. **Alkonyat / case studyk:** zoom out, ég derengésbe; diagonálisan 2+2 szomszéd épület, rajtuk NEON táblák: vanity/olive/cliff/matrix.
5. **Kapcsolat / telefonfülke:** utcai fülke = Kontakt; rákattint → űrlap; X → csörög; telefon → Matrix-élmény, X → vissza a tető-landoláshoz.
+ A menüpontok végigjátszás után a saját épület-oldalukon maradnak.

**Assetek kész:** `building.glb` (Blender-export→gltf-transform, 5,68MB, van külön „window" material az ablak-fényekhez), `public_phone_booth.glb` (5,86MB, optimalizálandó). A neon táblák + vanilla sky PROCEDURÁLIS.

**ACT 1 KÉSZ + user-mockup szerint belőve (2026-07-26):** `AgencyBuilding.jsx` + `.css` — vanilla sky shader (golden dusk + felhők + nap + `uNight` az éjszakához), épület `Box3`-normalizálva, helikopter-ereszkedés kamera (scroll `progressRef`) LENÉZ a tetőre. **A user mockupja szerint:** a masthead A TETŐRE fektetve (`RoofMasthead`, drei `Text` rotX=-90°, nem külön DOM-panel), „égőkörték" a tető szélén körben (`RoofBulbs`, emissive gömbök + bloom), a „WEB & STYLE / web experience studio" tábla sötét board-on a hátsó élen (`RoofSign`). **FONTOS FIX:** a GLTF-export az épület falait `alphaMode:BLEND`-re tette (átlátszott a fire escape a falon) → `Building` most kényszeríti opaque-ra (`transparent:false, depthWrite:true, transmission:0, FrontSide`). Bloom bekötve (`@react-three/postprocessing`, magas küszöb → csak a tábla/bulbok izzanak). **Fejlesztés alatt `?building`-gel nyílik** (a live `#agency` érintetlen). Screenshottal igazolva.

**ÉG ÁTHANGOLVA a 20th-Century referenciára (2026-07-26, user screenshotja szerint):** mély indigó-lila zenit (`gZen 0.12/0.09/0.30`) → viola-mályva közép → izzó narancs alsó ég; VASTAG gomolyfelhő-bank alacsonyan (szalmon/arany, a nap felé megvilágítva, árnyékos hasak violába), diffúz meleg nap-glow (nincs kemény korong), halvány sötétlila felső fátyolfelhők. Fények: narancs key + viola fill + hemisphere.

**ÉG 2.0 — „BENNE VAGYOK" felhőrendszer (2026-07-26, user-referencia szerint):** a user festett dusk-referenciájához igazított dóm-gradient (mély navy-indigó zenit → széles kék-viola középmező → barack horizont; halvány KEREK csillagok már alkonyatkor — gnomonikus projekció, nem nyúlnak el; nap-glow visszavéve, nincs kiégő korong; a horizont ALATTI void füstös rózsa-mályvába sötétedik). A mélységérzetet a **`CloudField`** adja: 42 procedurális felhő-billboard (canvas-textúrák) 4 rétegben — távoli alacsony bank / közép-gyűrű / magas fly-past / **FELHŐ-TENGER** (lapos, fekvő síkok az épület ALATT — a helikopter-ereszkedés felhőtetőkre néz le, nem üres égre). A függőleges rétegek **CILINDRIKUS billboardok** (csak Y-tengely körül fordulnak a kamera felé) — felülről élére fordulva rétegekként vékonyodnak el, nem képernyőt kitöltő ködfalként (ez volt az első verzió hibája). Éjszaka minden felhő sötét kékesszürkébe tintázódik + halványul. A user chatbe másolt referencia-KÉPE fájlként nem érhető el — ha bekerül a projektbe, a dómra cserélhető/keverhető.

**ÉG/ÉJSZAKA FINOMHANGOLÁS user-visszajelzésre (2026-07-26 este):** (1) az alkony-gradient GAZDAGABB lett („túl vanília" volt) — forró narancs horizont (`gHor 1.02/0.48/0.20`), telített viola közép (`gMid 0.40/0.22/0.60`), mély indigó-lila zenit + egy magenta lehelet a narancs–viola találkozásnál; a felhő-tintek is teltebbek (dóm-sáv + billboard színek). (2) Az éjszakai ég SÖTÉTEBB (`nHor 0.045/0.055/0.14`, felhők `#0f1322` + erősebb éjszakai halványulás ×(1−0.5n)). (3) Az épület éjjel VILÁGOSABB + **`StreetLights`**: 5 meleg nátrium-lámpa a talapzat körül (pointLight 7×night, distance 7.5, decay 2 + glow-sprite bokeh) — az első verzió (intensity 30) nappallá mosta az alsó szintet, visszavéve fénytócsákra; a `NightLights` éjjel erősebb hold-key/fill + meleg city-glow groundColor (`#2b1a0c`). Screenshotokkal igazolva (alkony-mászás + éjszakai ablak-stopok).

**ABLAK-IGAZÍTÁS A VALÓDI RÁCSHOZ (2026-07-26 este, user-bugreport):** az overlay-ablakok a téglára csúsztak. Megoldás: a GLB „window" materialjának vertexeit playwright+GLTFLoader scripttel kinyertem és klasztereztem (a `Building` normalizálásával azonos térben) → **a jobb fal ÜVEG-síkja x=3.158 (nem 3.62!), a rács 9 emelet × 8 oszlop** (sorok y≈1.01…10.14 lépés ~1.14; oszlopok z≈-3.0…2.89 lépés ~0.8; cella 0.30×0.57). A `WINDOWS` 5 pontos rács-cellára állítva, `WIN_X 3.19`, pane 0.30×0.57. Debug vonal-ráccsal (ideiglenes `?wingrid` réteg, törölve) screenshotról igazolva, hogy a sorok/oszlopok a látható nyílásokon ülnek. **Másik tanulság: a téglapillérek ~0.4-gyel az üvegsík ELÉ nyúlnak** — a fact-panel/szöveg ezért a falcsoporton belül lokál +z 0.55-re (világ x≈3.74) került, különben a pillérek csíkokban kitakarják. Az extrakciós script a repóban: **`tools/extract-windows.mjs`** (futó dev szerver mellett `node tools/extract-windows.mjs` a projekt-gyökérből — más falhoz a `maxX`-szűrőt kell átírni).

**VALÓDI, BELÜLRŐL VILÁGÍTÓ ABLAKOK (2026-07-26 este, user-kérés):** az overlay-pane + rárajzolt kereszt zavaró volt (nem úgy nézett ki, mint a többi ablak). Átépítve: (1) a `Building` traverse a **„window" materialt NEM kényszeríti opaque-ra** — átlátszó üveg marad (`transparent:true, opacity:0.38, depthWrite:false`), csak a falak opaque-ok; (2) az épületbe került egy **sötét belső „blackout" doboz** (BackSide box, 6.1×~11×6.1 — enélkül az üvegen át az égig látnál); (3) az 5 folyamat-fény most **meleg fény-sík az üveg MÖGÖTT, az épület belsejében** (lokál z −0.4, szín [1.75,1.32,0.78] toneMapped:false) — a VALÓDI ablaknyílás és a valódi tokosztás keretezi, így a világító ablak pontosan úgy néz ki, mint a sötétek, csak ég benne a lámpa; kívül csak halvány spill-glow maradt a téglán. Ez a korábbi 02-es „rossz helyen van" gondot is megoldotta (a nyílás keretez, nem az overlay). Nappal az ablakok a sötét belső tér miatt ugyanolyan sötétek, mint eddig (Act 2 változatlan). Screenshotokkal igazolva.

**PANEL-ÁTFEDÉSEK JAVÍTVA + Build-finálé átrendezve (2026-07-26 este, user-jelölés szerint):** az 1 emeletes függőleges távolságú stopoknál a 1.5 magas fact-panelek sarkai egymásra csúsztak → **az egymást követő stopok most 2 emeletnyire vannak** (sorok: 2.146 / 4.435 / 6.712 / 8.992 / 10.135), így a panelek geometriailag nem érhetnek össze. Az **5. stop (Build) a LEGFELSŐ sorra került** (10.135, közvetlenül a tető-perem alatt), és a **szövege a BAL oldalára** (per-ablak `s` oldal-mező + keskenyebb panel: `pw 2.6 / tw 2.3 / po 1.95`) — így vízszintesen is elkerüli az eggyel lejjebbi 04-panelt; a panel bal széle kicsit a homlokzat-él elé lóg (sötét ég előtt lebegő címke-hatás, vállalt). A `winSide` mostantól explicit `w.s`-t olvas (nem z-előjelből számol).

**ACT 4 ZÁRÓKÉP VÉGLEGESÍTVE + 2 ÚJ FONT (2026-07-27):** (1) A user az editorban belőtte a záró kamerát → **beégetve** a `CAM_KEYS`-be: `pos V(27.3, 9.6, -0.4), tgt V(0, 6.7, 0), fov 34`. Mivel a `CameraRig` eddig nem állította a fov-ot (végig 45), most a **fov is a kulcskockákból lerpel** (`a.fov ?? 45`), csak az Act 4 vége zoomol 34-re, a korábbi felvonások maradnak 45-ön. A `CAM_DEFAULT` is erre állítva (editor innen indul). (2) **Új feliratfontok** (mindkettő kereskedelmi használatra szabad — jobb, mint a Bastliga demó): **Vanity → Mochesa** (1001Fonts FFC, `Mochesa.ttf`), **Olive → Astra Style** (SIL OFL, `AstraStyle.ttf`). A Build még Bastliga (demó — csere/licenc később). (3) **Tábla-takarás javítva a magasabb kamerából:** a user editor-nézetében a PANEL takarta a jobb oldalt, ezért nem látszott, hogy az Olive félig a hero-masthead mögé esik. Az Olive-t jobbra (`z −8`), a MATRIX-ot messzebbre (`z −15.8`) toltam — így a jobb oldal szeparációja a bal (CLIFF↔Vanity) ritmusát tükrözi, és mind az 5 tábla teljesen olvasható. Screenshottal igazolva.

**KAMERA-EDITOR + TÁBLA-RE-ANCHOR (2026-07-27):** (1) A táblák elcsúsztak, mert amikor a süllyesztett szomszédokat visszaemeltem talajszintre, a tetővonalak megnőttek, de a `NEON_SIGNS` Y-ja nem követte. **Javítva:** minden tábla Y-ja mostantól a saját épület tetővonalából számolt (`11 × scale + poles + fél board-magasság`), a pos X/Z pedig az épület középpontja → a tetőn ülnek, bármekkora is az adott ház. (2) **Új kamera-editor: `?building&cam`** (`CameraEditor` + `CAM_EDIT` flag `AgencyBuilding.jsx`-ben). A jelenetet a záró Act 4 nézetre FAGYASZTJA (`progressRef=1`, `night=1`, scroll kikapcsolva), és a kamerát 6 csúszkával (pos XYZ + néz/target XYZ) + FOV-val lehet élőben állítani (`CameraRig` `editRef`-ágon olvassa a `camEditRef`-et minden frame-en). Élő JSON-kimenet a `CAM_KEYS` utolsó kulcskockájának formájában + „JSON másolása"/„Mentett"/„Reset" gombok, localStorage-persist (`ws-building-cam`). A user belövi, JSON-t küld, beégetem a `CAM_KEYS` `p:1.00` kulcsba. Screenshottal igazolva (panel renderel, csúszka élőben mozgatja a kamerát).

**TETŐ-MASTHEAD FONTCSERE (2026-07-27):** a user 2 saját fontot adott a tetőn olvasható szöveghez (`RoofMasthead`, `AgencyBuilding.jsx`): a headline ("Behind the system, a studio." / HU megfelelője) mostantól **Jogrunge** (`jogrunge.zip` → `public/assets/fonts/Jogrunge.otf`, grunge-hatású display face), a törzsszöveg (intro-bekezdés) **Penna** (`penna.zip` → `public/assets/fonts/Penna.otf`, kézírásos script face) — mindkettő új `ROOF_HEAD_FONT`/`ROOF_BODY_FONT` konstans, a `RoofSignBoard` (a tető szélén álló "WEB & STYLE" tábla) változatlanul a `Metropolis-Medium`-ot használja. A szövegszín mindkét rétegen `#231710`/`#3a2a1e` (barna) helyett **fehér** (`#ffffff`) lett. Screenshottal igazolva (`?building`, a belső `.abx__scroll` konténer görgetve ~7%-ra, ahol a masthead teljesen bejön) — a headline jól olvasható texturált grunge-stílusban, a törzsszöveg finom kézírásos kontrasztot ad, mindkettő tisztán fehéren üt el a sötét tetőtől.

**A TETŐ-FONTPÁR MINDENHOL (2026-07-27, user-kérés: "mindenhol változtasd meg... arra, amit a tetőnél alkalmazunk"):** a `ROOF_HEAD_FONT` (Jogrunge) / `ROOF_BODY_FONT` (Penna) / fehér szín párost, amit a tető-masthead-hez vezettünk be, kiterjesztettem minden headline+body szövegpárra az épület-élményben: **Act 2** (`FloorFacts`, a bal falon futó "What I build" 8 tény-panel — `s.title`→Jogrunge, `s.text`→Penna, mindkettő fehér, fontSize kicsit emelve 0.2→0.24 / 0.115→0.145 a Jogrunge/Penna eltérő vizuális súlya miatt), **Act 3** (`ProcessWindows`, a jobb falon az 5 folyamat-panel — ugyanaz a csere, 0.21→0.25 / 0.12→0.15), és a repülő **"These, I" tagline** (Metropolis→Penna, krém→fehér, 0.32→0.4). **Szándékosan ÉRINTETLEN maradt**: a `RoofSignBoard`/tető szélén álló "WEB & STYLE" tábla (brand-wordmark, nem tartalom-szöveg, Metropolis marad — ez már a tető-masthead bevezetésekor is külön kategóriaként lett kezelve), a "01"–"05" szám-kickerek (kis UI-címke, nem headline/body), és a case-study neon-táblák (Vanity/CLIFF/Olive/MATRIX — ezeknek saját, márka-specifikus fontjuk van, ld. korábbi bejegyzés). Screenshottal igazolva mindkét Act (2 és 3) — a grunge headline + kézírásos body pár jól olvasható a fal-panelek sötét hátterén is.

**VÁROS-HÁTTÉR: TARTÁS ACT4-IG + NAPPAL/ÉJSZAKA (2026-07-28, user-kérés):** két bővítés: (1) a `CityBackdrop` korábban az Act 2 mászás előtt tűnt el — a user kérte, hogy maradjon bent **egészen addig, amíg a case-study szomszéd-épületek (a saját tábláikkal) meg nem jelennek** → a fade-out most `ACT4_IN` (0.78) köré került (`1 - sm(p, ACT4_IN-0.03, ACT4_IN)`), pontosan összeérve a `NeighborCity` saját megjelenési küszöbével (`p > ACT4_IN`) — a váltás az arany villanás fényébe rejtve történik, nincs üres pillanat háttér nélkül. (2) **A város most a scene éjszaka-értékét követi** (`skyRef.night`, ugyanaz a jel, ami a `NightLights`/`CloudField`-et is vezérli): minden anyag eredeti ("nappali") színét eltároltuk (`m.userData.dayColor`), és mostantól minden frame-ben `m.color.lerpColors(dayColor, CITY_NIGHT_TINT, night)` — hűvös, sötét kékes tónusba fordul éjszaka (`CITY_NIGHT_TINT = #232a45`), ugyanúgy, ahogy a felhők/ég is elsötétednek. Screenshottal igazolva: nappali landolás, éjszakai Act 3 (a város a háttérben hűvös-sötét, jól passzol az éjszakai éghez), és az Act4 villanás-pillanat (a város már eltűnt, a szomszéd-épületek még nem világítanak — tiszta handoff).

**VÁROS-HÁTTÉR FINOMHANGOLVA (2026-07-28, user élőben belőtte az `?building&city` editorban):** a user visszaküldte a saját kalibrált értékét → **`CITY_DEFAULT = { pos:[6,0,-17], scale:0.0009, rotY:0 }`** beégetve. Emellett jelezte, hogy a város a **legelső képkockán (p=0, a madártávlati leszállás kezdetén)** is bent kéne legyen, nem csak a tető-tartásnál — az eredeti fade-in (`sm(p,0,0.03)`) miatt pontosan p=0-nál még 0 volt az opacitás. **Javítva:** a fade-IN teljesen törölve, a város mostantól **azonnal, teljes opacitással** jelen van a scroll legelejétől, csak a fade-OUT maradt (`1 - sm(p, ROOF_HOLD+0.01, ACT2_IN-0.01)`, változatlanul az Act 2 mászás előtt tűnik el). Screenshottal igazolva mindkét korai állapot (p=0 madártávlat + p=0.08 tető-tartás) — a heró most egy valódi városi nyílásban landol végig, nem üres térben.

**VÁROS-HÁTTÉR A TETŐ-LANDOLÁSHOZ (2026-07-28, user-kérés: "ne lebegjen légüres térben az épület az elején"):** a user egy ingyenes Sketchfab-city-kitet adott (`futuristic_city.glb`, `sketchfab.com/3d-models/futuristic-city-...`, 12,5MB, 8 mesh, ~179k tri) → bemásolva `public/assets/models/futuristic_city.glb`. **Új komponens: `CityBackdrop`** — csak a tető-landolás (Act 1, `ROOF_AT`/`ROOF_HOLD`) alatt látszik, elhalványul, mire az Act 2 bal-fal mászás elindul (`sm(p,0,0.03) * (1-sm(p,ROOF_HOLD+0.01,ACT2_IN-0.01))`, minden anyagon egyszerre animált opacity). **Az utak aszfaltszínre javítva:** a kit két hatalmas, azonos kiterjedésű lapos meshét (`Wax_02` és `Default1` anyagnév — headless GLTFLoader-vizsgálattal azonosítva, a user által mutatott Sketchfab-screenshoton látott bézs/beton út-textúra forrása) map nélkülire + sötét aszfalt-színűre (`#15161a`) állítottam, a járdák (`default_sidewalks`) és az épületek anyaga érintetlen. **A modell mérete irreális** (nyers egységekben ~169 000×34 000×152 000) — nincs értelmes "helyes" skála/pozíció, amit ki lehetne számolni, ezért a user kérésének megfelelően **élő editort** kapott, nem vak becslést: **`?building&city`** — a `CameraRig`-től függetlenül a jelenetet a tető-tartás (`ROOF_HOLD`) kameraállásra fagyasztja, és egy `CityEditor` panelt mutat (pos X/Y/Z, méret, forgás csúszkák + élő JSON-kimenet + Mentett/Reset gombok, `ws-building-city` localStorage-perzisztencia) — ugyanaz a minta, mint a meglévő `?building&cam` kamera-editornál. **Kezdő becslés** (`CITY_DEFAULT = { pos:[0,-2,0], scale:0.0007, rotY:0 }`) már screenshottal igazoltan jól illeszkedik (a hero középen ül a város egy nyílásában, arányos épületmagasságok) — ha a user mégis finomítani akar rajta, az editorban élőben teheti, majd elküldi a JSON-t, és beégetem a defaultba. Screenshottal igazolva mind az editor-nézet, mind a valódi scroll-lejátszás (roof-hold: város látszik körben; Act 2 climb: teljesen eltűnt).

**FINÁLÉ v2 — ABLAKFÉNYEK A VALÓDI NYÍLÁSOK MÖGÉ + BUILD ÉLESÍTVE/KORÁBBRA + DÚSABB POR (2026-07-28, user-visszajelzés 3 screenshottal):** (1) **Az extra ablakfények a szomszédokra kerültek, a valódi ablaknyílások MÖGÉ** (nem a falra ragasztva) — a user kifogása: "csak az első épület ablakaiba építettük be a fényt, oda nem kell több; a többibe kell, az ablakok MÖGÉ NEM a falra". A hero extra-ablakait teljesen levettem (marad az 5 Act 3 ProcessWindows). A szomszédok kamera felé néző fala a **lokál +z fal** (a ~90°-os elforgatás miatt a +x rács oldalra néz — ezért nem látszottak korábban) → kinyertem a **valódi +z ablakrácsot** (`tools`-mintájú max-z extractor: fal z≈3.06, 6 belső oszlop x∈{-1.97…2.09}, 9 emelet). Épületenként 6 ablak (`LIFE_CELLS`), **meleg belső fénysík a nyílás mögött** (`WIN_Z_RECESS = 3.06-0.4`), a `NeighborCity` neighbor-groupjának GYEREKEKÉNT (örökli pos/ry/scale → pontosan a valódi ablakra ül). **Kulcs-felismerés:** a `scene.clone(true)` MEGOSZTJA a materialt, és a `<Building>` már átlátszóvá tette a "window" materialt → a szomszédok üvege is átlátszó, a meleg sík ÁTLÁTSZIK a valódi nyíláson, a valódi ablakkeret keretezi (`LifeWindow` komponens, ugyanaz a `[1.75,1.32,0.78]` meleg szín, mint a heró Act 3 szobafénye). Screenshottal (2× dpr közeli zoom) igazolva: a fények a valódi ablakok mögött ülnek, kerettel/tokosztással — prémium, nem decal. (2) **A Build szó szaggatottsága javítva** (`sdfGlyphSize={128}` — a vékony neon-vonalak nagy skálán már nem frányulnak/pixelesednek), és **korábban ér középre** (`BUILD_FLY_END` 0.93→0.865), **hosszabban tart/ragyog** (`BUILD_HOLD_END` 0.925). A kamera-időzítés is előrehozva (wall-view 0.80 → végső keret 0.83 = `BUILD_FLY_START`), a stabil-kamera-előbb elv megtartva. (3) **A por-szétporlás dúsabb/prémiumabb** (a user szerint "béna lett"): `DUST_PER_TARGET` 2→7 (≈200 részecske), **egyenes vonal helyett ívelt bezier-pályák** (lofted control point → organikus rajzás), + **táguló shockwave-gyűrű** a burst pillanatában (`ringRef`, kamera felé fordul, additív). Screenshottal igazolva mind: közép-tartás (éles Build) → burst → sűrű arany embercsík-rajzás ívekben → staggerelt landolás (Vanity/CLIFF már ég, Olive/MATRIX-ra még repül) → végállapot (mind a 4 szomszéd valódi ablakai + a 4 tábla világítanak). **Megjegyzés:** a Build "B" betű pontozott flourish-e a Bastliga font sajátja (nem renderhiba) — ha a user ezt is szaggatottnak érzi, fontcsere lesz a következő lépés.

**BUILD-FINÁLÉ (v1, 2026-07-27, user-vízió) — ELŐZMÉNY:** a user 3 dolgot kért egy screenshot-hármas alapján: (1) a tető-tábla ("WEB & STYLE") még mindig túl keskeny volt, a szöveg túllógott — `RoofSignBoard` plane szélesség 5.6→7.2. (2) a fal-villanás ("fénygömb") "béna" volt (sima elmosott kör) — **`makeBurstTexture()`** új függvény: forró mag + lágy glow + 8-ágú csillag-fler (canvas 2D, additív spike-gradiensek), sokkal prémiumabb lencsefler-hatás, lassan forog + pulzál a burst alatt. (3) **A Build teljes végjátéka újratervezve**, sokkal több idővel: fal-izzás után **NEM azonnal repül el** — a kamera előbb **EGYETLEN mozdulattal** eléri a végső, teljesen statikus keretezést (`CAM_KEYS`, `BUILD_FLY_START=0.885`-ig), csak utána **repül fel Build a kép KÖZEPÉRE** (`BUILD_CENTER_POS`, nagyban, ~1.7x skálán, `BUILD_FLY_END=0.93`), ott **tovább nő és ragyog** (`BUILD_HOLD_END=0.95`, "adjunk több időt hogy felragyogjon" — user-kérés), majd **szétporlad** (`BUILD_BURST_END=0.962`, második, nagyobb prémium-fler-impulzus pontosan ekkor). **Fontos hiba, amit menet közben találtam és javítottam:** az első verzióban a repülés ÁTFEDÉSBEN volt a kamera saját dolly-back mozgásával — mivel mindkettő egyszerre mozgott hasonló térben, Build óriásira nőtt útközben (mintha a kameránál landolt volna egy pillanatra). Megoldás: a kamera MINDIG álljon meg ELŐSZÖR (0.84→`BUILD_FLY_START`, egy mozdulat), és csak utána kezdjen repülni Build egy már teljesen stabil, lezárt képkockán belül — ez egyben tisztább filmes nyelv is (gyors reveal-pull, majd hosszú, kizárólag a payoff-ra fókuszáló locked-off felvétel).

A szétporlás **arany por-kaszkádot** indít (**`GoldDustBurst`**, `THREE.Points`, 36 részecske, 2/célpont), ami **mind a 4 case-study neontáblára** (Vanity/CLIFF/Olive/MATRIX — a `NeonSign` ignite-ideje mostantól a por érkezéséhez van kötve, nem önálló `ACT4_NEON`-hoz), **mind 14 ÚJ, "extra" ablakra** repül szét — 6 a hero épületen (a valódi ablakrács üres celláit újrahasznosítva, `EXTRA_WIN_CELLS`), 8 a 4 szomszéd-épületen (2-2, a normalizált lokál-koordinátákat a szomszéd saját position/rotation.y/scale-én átvezetve, `neighborWorldPoint`) — ez a user kért "mintha egy városban lennénk, ahol még vannak, akik ébren vannak éjszaka" hatás: a fények egyenként, staggerelve gyulladnak ki (`igniteArrive(idx)`, egy közös érkezési ütemterv mind a 18 célponton). **Két finomítás menet közben:** (a) az ablak-szikrák eredetileg a kiálló téglapillérek MÖGÉ estek (a valódi ablakok is a fal síkjától 0.4-gyel beljebb, "recessed" vannak) → `WIN_RECESS = WIN_X - 0.4`; (b) a szikrák eredetileg kamera-billboardként forogtak, ami ferde szögben belelógott a pillérekbe (félhold-levágás) → statikus rotáció lett, a fal saját irányát követve (`rotation={[0, ry, 0]}`), akárcsak a valódi ablakfényeknél. A `taglineRef`/"These, I" felirat-koncepció (előző körben épült) **törölve** — a user új víziója (fényként szétszóródó Build) felülírta, a `content.js` `buildTagline` mezőjével együtt eltávolítva.

Screenshot-sorozattal igazolva mind a 4 fázis: fal-izzás → stabil kameránál nagyban középen tartás/ragyogás → prémium csillag-fler szétrobbanás → por-kaszkád (apró repülő fénypontok) → végállapot (mind a 4 tábla + a hero + mind a 4 szomszéd épület extra ablakai világítanak, a "város felébredt").

---

## 🗂️ Menüpontok / navigáció — feltárás a usernek (2026-07-27)

A user rákérdezett: milyen menüpontok/témák vannak jelenleg, hogy el tudja dönteni, hogyan helyezzük el őket. **Nincs még kiépített nav-komponens** — a `content.js` `AGENCY` objektuma viszont már tartalmazza az összes tartalmi szekciót, ezek közül némelyik MÁR van fizikai helye az épületben, némelyiknek MÉG NINCS:

| Szekció (`AGENCY.*`) | Tartalom | Fizikai hely az épületben |
|---|---|---|
| `title`/`intro` | Landolás-üzenet | **KÉSZ** — tető-masthead (Act 1) |
| `servicesTitle`/`services` (8 db) | "Amit építek" | **KÉSZ** — bal fal, emeletenként (Act 2, `FloorFacts`) |
| `processTitle`/`process` (5 db) | "Hogyan dolgozom" | **KÉSZ** — jobb fal, ablakonként (Act 3, `ProcessWindows`), az 5. ("Build") a por-kaszkád triggere is |
| `workTitle`/`work` (4 db: Vanity Mirror, House on the Cliff, The Story of the Olive, Web & Style immerzív élmény) | "Kiválasztott munkák" | **RÉSZBEN** — csak a NÉV van kint neontáblaként (Vanity/CLIFF/Olive/MATRIX) a szomszéd épületeken; a leírás/role/kép/link (`work[].text/role/desktop/href`) **még sehol nincs megjelenítve** — nincs kattintható/felnyitható munka-részlet |
| `aboutTitle`/`about` | "Ki áll mögötte" (Alex bemutatkozása) | **NINCS HELYE MÉG** |
| `contactTitle`/`contactText`/form mezők | "Kezdjünk bele" (kapcsolat-űrlap) | **NINCS HELYE MÉG** — az 5. felvonás (utcai telefonfülke = kapcsolat) a tervben szerepel, de nincs megépítve |

**Nyitott kérdés a usernek:** az About és a Contact hova kerüljön (van-e már elképzelés, pl. a telefonfülke tényleg megépül-e, vagy más helyszín/interakció), és a Work-panelek hogyan nyíljanak fel részletesre (kattintásra teljes képernyős panel? külön "landolás" az adott szomszéd-épületnél?).

---

**FACT-PANEL DOBOZOK NAGYÍTVA + WEB & STYLE TÁBLA IS A TETŐ-FONTPÁRON (2026-07-27, user screenshot-bugreport):** a user két screenshotot küldött (Act 2 nappal + Act 3 éjjel), és jelezte: (1) a sötét háttér-téglalapok túl kicsik lettek, miután a Jogrunge/Penna fontméretek megnőttek — a body-szöveg (különösen 3-soros leírásoknál) kilógott a doboz alján, ki a puszta téglafalra. (2) az Act 2 (nappali, `FloorFacts`) panelokon nagyobbnak tűnt a sorköz, mint az Act 3 (éjszakai, `ProcessWindows`) panelokon. **Ok:** a cím-szövegek (`Text`) nem kaptak explicit `lineHeight`-ot, a Jogrunge betűtípus saját, lazább alap-sortávot használt — 2-soros törésnél (pl. "Frontend & full-stack development") ez feltűnően nagyobb rést hagyott, mint az Act 3 példákon, ahol a cím épp nem tört 2 sorba. **Javítva:** mindkét panel-típuson a cím `lineHeight={1.05}` lett (a tető-masthead sajátja), a háttér-téglalapok magassága nőtt (`FloorFacts` 1.18→1.55, `ProcessWindows` 1.5→1.7, a középpont-Y is arányosan lejjebb tolva), és a két oldal fontméretei EGZAKT azonosra kerültek (cím 0.24, törzs 0.145, mindkét helyen). (3) **A tető szélén álló "WEB & STYLE / WEB EXPERIENCE STUDIO" tábla** (`RoofSignBoard`) is a tető-fontpárra váltott (Jogrunge cím / Penna alcím) — user-kérésre kifejezetten ez volt a korábbi kivétel, most megszűnt —, **de a színek/emissive-glow változatlanok maradtak** (meleg krém/borostyán, nem fehér — user explicit kérése: "a színek viszont maradjanak"). Screenshottal igazolva mindhárom hely (Act 2, Act 3, tető-tábla) — a dobozok most kényelmesen tartalmazzák a 2-3 soros szövegeket, a sorköz vizuálisan egyezik a két fal közt.

**ACT 4 — ÉPÜLETEK CSAK A VILLANÁSKOR + REPÜLŐ "BUILD" TAGLINE (2026-07-27, user-visszajelzésre):** két bugreport egy screenshot-párral: (1) a törzsszöveg (Penna) a tető-masthead-en alig látszott → fontSize 0.23→0.3, maxWidth 5.6→6.0, lineHeight 1.4→1.3. (2) a `NeighborCity` (a 4 szomszéd-épület + neonjaik) korábban `progressRef>0.74`-nél jelent meg — ez A VILÁGÍTÁS/villanás ELŐTT volt, a user egy konkrét screenshoton be is karikázta a idő előtt látszó szomszéd tetőt (a Build-fal-dolly alatt, mert a kamera-pozíció változatlan 0.7465→0.83 közt, csak a fact-panel "05 Build" felirata volt még nem-kézírásos). **Javítva:** a küszöb `ACT4_IN`-re (0.78) került — pontosan ott, ahol az arany villanás (`NeonBuildWord` flash) elindul, így a felvillanó fény TAKARJA a pop-in-t (a `StreetBlock` utca-aljzat fade-timing-ja is ugyanerre igazítva, hogy ne látsszon üres utca a épületek előtt). (3) **Új szerep a kézírásos "Build"-nek:** a user kérése szerint amikor a fal-villanás után kézírásossá válik, repüljön fel az épületek fölé és egészítse ki a mondatot ("These, I ⏎ Build" — HU-ban "Amit építettem" felirat + "Megvalósítás" szó, mert a hu főnévi alak nem olvad egy mondatba). Megépítve: a `NeonBuildWord` csoportja a fal-pozícióból (`BUILD_WALL_POS`) a `BUILD_SKY_POS`-ba lerpel (`BUILD_FLY_START 0.855 → BUILD_FLY_END 0.94`, a kamera pont ekkor dollyol vissza), közben egyenletesen felskálázódik (`BUILD_SKY_SCALE 2.0`, mert a záró kamera ~5x messzebb van, mint a fal-közeli dolly). A "These, I" felirat (Metropolis font, halvány krém) csak a landolás végén fade-el be. **Két iteráció kellett a pozícióra:** először a hero saját tető-tábla OSZLOPÁBA (mögé) került (elfedte az opak tábla-háttér) és/vagy kilógott a képkeret tetején (fov 34 szűk keretezésénél alig van hely a hero-tábla fölött) → végül az Olive/MATRIX (alacsonyabb tetejű szomszédok) fölé, oldalra tolva (`BUILD_SKY_POS = V(-8.7, 13.4, -9.5)`) — így szabad égen landol, semmivel nem ütközik nyugalmi helyzetben (repülés KÖZBEN egy pillanatra áthalad a hero-tábla mögött, de ez csak átmeneti, elfogadható). Screenshot-sorozattal igazolva mind a négy fázis (villanás előtt — nincs épület; villanás — a fény eltakarja a pop-int; fal-izzás — "Build" tisztán a falon; repülés vége — "These, I / Build" tisztán az Olive/MATRIX fölött).

**ACT 4 v5 — SÖTÉT UTCA-ALJZAT (2026-07-26 éjjel, user-kérés: "kéne valami sötét aljzat, utca"):** eddig a blokk a felhőtenger fölött lebegett — most valódi utca-szintre került. **`StreetBlock`** (`AgencyBuilding.jsx`): (1) sötét, nedves-hatású aszfalt-lap (`MeshStandardMaterial`, roughness 0.38 — ténylegesen fényt fog a lámpáktól/neonoktól, nem unlit), (2) betonjárda-keret körben (világosabb szürke, érdes), (3) kontakt-árnyék minden épület lába alatt (`makeShadowTexture` — sötét radiális folt, hogy a blokk a talajon ÁLLÓNAK tűnjön, ne lebegjen), mind a szomszédokkal egy ütemben oldódik be (`sm(p,0.70,0.80)`). **Két iteráció kellett:** az első verzió (21×40-es lap) túl kicsi volt — a záró kamera (x≈27) messze túllát a lap szélén, ami egy csúnya, csillagos ég ellen úszó "ék-lyuk" volt. **Javítva:** egy 260×260-as, láthatatlanul nagy sötét alap-sík mindenki alatt (a részletes járda-blokk ezen ül, mint kiemelt terület) + **scene-szintű `<fog>`** (`#0d0f1c`, near 55 / far 170 — a dóm és a felhő-billboardok `fog={false}`-lal kimaradnak), hogy a hatalmas síknak sose legyen éles pereme az égen. A `NEIGHBORS` y-eltolása (a távoli pár korábban a felhőbe volt süllyesztve) visszaállítva 0-ra, mert most már valódi talajszinten állnak.

**ACT 4 v4 — ROOF-SIGN CSALÁS + PRÉMIUM SKYLINE (2026-07-26 éjjel):** (1) a WEB & STYLE tetőtábla a dolly-out alatt (p 0.80→0.94) **észrevétlenül 90°-ot fordul a tető tengelye körül** (`RoofSign` rig + `RoofSignBoard`), így a záró frontális kamerával SZEMBEN áll a tető túlsó élén — klasszikus filmes csalás, mozgás közben nem tűnik fel. (2) A szomszéd-elrendezés prémiumra hangolva: minden tetővonal MÁS magasságon lépcsőzik (hero 11 → CLIFF 10.8 → Olive 9.9 → Vanity 9.0 → MATRIX 7.3), a távoli pár enyhén a felhőtengerbe süllyesztve (y −0.6/−0.8), a laterális távolságok szándékosan aszimmetrikusak; a táblák és lámpák követik.

**ACT 4 v3 — ÉK-ELRENDEZÉS + BASTLIGA FONT + VALÓDI LÁMPAFÉNYEK (2026-07-26 éjjel, a user 2. Blender-mockja szerint, screenshottal igazolva):** (1) **Font:** a kézírás mostantól **Bastliga One** (`public/assets/fonts/BastligaOne.ttf`, a gyökér `bastliga-one.zip`-ből) — szignó-stílusú, nagy swash-ekkel; **⚠️ DEMO licenc: PERSONAL USE ONLY — éles indulás előtt kereskedelmi licenc kell** (creativemarket.com/MadhalineStudio; a kódban komment jelzi). A Mr Dafoe megmarad a fonts mappában tartaléknak. (2) **Ék-elrendezés:** a hero elöl-középen (a Build ablak a horgony, onnan indul a dolly-out), mögötte 2-2 szomszéd ÉK-ben hátrafelé (`NEIGHBORS` x −5.2..−11.5, z ±4.6..±12.5, kifelé döntve ry π/2±0.45..0.55, a közeli pár alacsonyabb s0.85) — az új közeli szomszédok részben a hero TAKARÁSÁBAN (szándékos átfedés, sosem látszik a belemetszés). Táblák lépcsőzött magasságban + szélesebb boardokon (a Bastliga-swashek miatt), oszlopokkal ahol kell — így az 5 világító szó sosem ér össze. (3) **Lámpák:** a szomszéd-lámpák már NEM kamu lebegő pöttyök — a bokeh-sprite-ok a homlokzatokhoz simulnak és **4 valódi pointLight** (szomszédonként 1) meleg fénytócsát vet az alsó téglasorokra (összesen 9 pointLight a jelenetben — mobil-perf később ellenőrizendő).

**ACT 4 ÁTÉPÍTVE A USER BLENDER-MOCKJA SZERINT (2026-07-26 késő este, screenshottal igazolva):** (1) **A Build-átalakulás javítva** — nem külön, kamerát követő elem többé: az eredeti Build-fact egy **fényvillanással kiég** (`NeonBuildWord` — glow-sprite flash `ACT4_IN`..+0.045, a fact-szövegek a villanás alatt tűnnek el, `factOut` +0.018-ra szűkítve), és a helyén, a FALON világ-horgonyzottan marad a kézírásos Build (nem mozdul; később eldöntjük, előrébb jöjjön-e). (2) **Frontális záró-kompozíció:** a végső kamera EGYENESEN a jobb falra néz (dolly-back a fal normálisa mentén: 0.885 → `V(26.5, 6.8, 1.8)`), a 4 szomszéd a főépület MÖGÖTT kétoldalt lépcsőzve áll (`NEIGHBORS` x −4.5..−9, z ±8.5..±17, mind ry≈π/2, kamera felé fordítva), a neon táblák is π/2-vel a kamerának. Balra CLIFF (hátul) + Vanity (elöl, oszlopos billboard), jobbra Olive (elöl) + MATRIX (hátul). (3) **Utcai lámpák kiterjesztve** minden szomszédhoz (`NEIGHBOR_LAMPS` — 8 lágy bokeh-sprite a tövekben, extra pointLight nélkül; a hero 5 lámpája adja a valódi fényt).

**ACT 3→4 HÍD + ACT 4 NEON-VÁROS MEGÉPÍTVE (2026-07-26 este, screenshottal igazolva):** scroll 1600vh → **2000vh** (korábbi p-értékek ×0.8; új konstansok: `ACT4_IN 0.78 / ACT4_NEON 0.86 / ACT4_OUT 0.97`). **A híd:** a Build-stop után minden fact-szöveg kioldódik (az ablakok égve maradnak), és a Build szó **kézírásos neonként** gyúl fel a képernyő közepén (`NeonBuildWord` — kamera-relatív, `getWorldDirection`×9 távolságra + kamera-quaternion, így a hátrahúzás alatt is középen marad; neoncső-stotyogós felgyulladás determinisztikus p-alapú flickerrel; a végén 0.965–0.995 közt kifakul). Font: **Mr Dafoe** (`public/assets/fonts/MrDafoe-Regular.ttf`, Google Fonts, SIL OFL — letöltve, a projekt fontjai közt nem volt kézírásos). **Act 4:** a kamera a Build ablakról hátrahúz széles éjszakai városképbe (`CAM_KEYS` 0.885/0.97/1.0), és megjelenik **4 szomszéd épület** (`NeighborCity` — a building.glb klónjai diagonálisan, saját blackout-dobozzal, `visible` csak p>0.74-től, amíg a falközeli képet a főépület tölti ki) **case-study NEON táblákkal** (`NeonSign`, lépcsőzetes felgyulladás `ACT4_NEON`+i·0.028, flickerrel): **Vanity** (pink script, TETŐRE EMELT oszlopos billboard — enélkül a főépület sziluettje takarta), **CLIFF** (cián, Metropolis), **Olive** (borostyán script), **MATRIX** (zöld, Jelvion). A táblák még nem kattinthatók — az interakció (case-study megnyitás) külön lépés. Acts 5 (telefonfülke/kapcsolat) hátra.

**ACT 3 KÉSZ (2026-07-26, screenshottal igazolva):** scroll-tér 900vh → **1600vh**, az Act 1–2 p-értékek átskálázva (`ROOF_AT 0.08 / ROOF_HOLD 0.125 / ACT2_IN 0.17 / ACT2_OUT 0.46`). A bal fal tetejéről a kamera az épület ELŐTT elhúzva a JOBB oldalra lendül, közben **beáll az éjszaka** (`NIGHT_IN 0.47 → NIGHT_OUT 0.56`, `skyRef.night` a scroll-tickből; `NightLights` — a key/fill/hemisphere fények intenzitása+színe éjszakaiba lerpel; a dóm éjszakai gradientre + teljes csillagmezőre vált, felhők sötétednek). **5 folyamat-fact 5 felgyulladó ablakban** (`ProcessWindows`, `WINDOWS` y 2.6→8.4, váltakozó z): az épület egyetlen mesh (ablakonként NEM bontható — gltf-transform inspecttel ellenőrizve), ezért ablak-OVERLAY: lágy radiális halo-sprite (`makeGlowTexture`) + valódi ablakméretű glow-pane (0.42×0.56) + sötét osztókereszt; mellette a fact sötét panelen. A kamera ablakról ablakra dollyzik (`WINDOW_KEYS`, arrive+hold kulcsok), az ablak a megérkezés ELŐTT gyullad fel, a fact utána úszik be, és minden égve MARAD (a menüpont a saját oldalán él tovább). Acts 4–5 hátra.

**ACT 2 KÉSZ (2026-07-26, screenshottal igazolva):** kulcskockás kamera-track (`CAM_KEYS`: madártávlat → tető-olvasás (hold 0.14–0.22) → átrepülés a BAL oldal földszintjére (p 0.30) → emelkedés a fal mentén a tetőig (p 0.82)); a kamera közel merőlegesen néz a bal falra, hogy a panelek szemből olvashatók. **8 „What I build" fact emeletenként** (`FloorFacts`): 3D Text panelek a bal homlokzaton (x=-3.62, emeletenként +1.06, váltakozó z ±1.75), sötét backing-plane + szám/cím/szöveg; mindegyik AKKOR úszik be, amikor az emelkedő kamera eléri az emeletét (`factReveal`), és utána KINT MARAD (a user kérése: a menüpontok a saját oldalukon élnek tovább). A tető-masthead kifade-el, ahogy elhagyjuk a tetőt. Scroll-tér 900vh. Acts 3–5 hátra.

## ✅ Kész (legutóbbi → régebbi)

- **A PIROS PIRULA szála LEZÁRVA (2026-07-26):** a szingularitás végi redirect a branded `https://www.webandstyle.com/`-ra megy (a vercel-alias helyett). A teljes lánc (vihar → kóddá válás → RGB drain-örvény → feketelyuk → fekete → redirect) kész és jóváhagyott.
- **A KÉK PIRULA — `#agency` stúdió-oldal MEGÉPÍTVE (2026-07-26):** a piros tükre — „a rendszer renddé áll össze". Új fájlok: **`Agency.jsx` + `Agency.css`**, tartalom a `content.js` **`AGENCY`** blokkjában (HU+EN, `useT()`). App-szintű hash-routing: a `#agency` hash egy teljes képernyős, csillapodott, prémium átvevő-oldalt mountol a journey fölé (a kék pirula / választás-kártya állítja a hasht); `onClose`/Esc/„Back to the experience" törli a hasht → vissza az élménybe. **Design:** sötét-lila alap + cián (kék-pirula) akcent, mono CSAK célzottan (számok/kickerek), csillapodott rendezett kód-rács háttér (nem eső). **Szekciók:** masthead → Szolgáltatások (8-as capabilities-index, nem kártyák) → Folyamat (5 lépés) → Kiválasztott munkák (Web & Style + The Vanity Experience, kromatikus keret-placeholderek) → Alexről → Kapcsolat (űrlap + `alex@webandstyle.com`). **Kapcsolati űrlap:** `FORM_ENDPOINT` const (üres → prefilled `mailto` fallback, ma is működik; ha Alex bepasztol egy Formspree/Resend URL-t, POST-ol), idle/sending/sent/error állapotok. Reveal-on-scroll (IntersectionObserver). **A CC-BY-4.0 attribúció** a footerben ("Chromatic Journey — Tycho Magnetic Anomaly · Sketchfab · CC-BY-4.0") — ezzel a függő tétel megoldva. Screenshottal végigigazolva. **MÉG HÁTRA:** a „code crystallizes into order" belépő-átmenet (jelenleg finom blur/scale fade a belépő) — opcionális signature-polish.
- **A SZINGULARITÁS + navigációs handoff (2026-07-25):** a vihar utáni teljes payoff-lánc (`Singularity.jsx`) + a `window.location` redirect a webandstyle.com-ra. Részletek fent, a "Most éppen ezen dolgozunk" alatt. Kóddá válás (per-material `onBeforeCompile` glyph-dissolve minden objektumon + talajon), shader-örvény (`SingularityVortex`), feketelyuk, redirect. `?arrival` "Szingularitás" szekció.
- **Chromatic Journey OPTIMALIZÁLVA (2026-07-25):** gltf-transform pipeline (weld → 0.6 simplify → webp textúra → KHR_mesh_quantization) → **18,4MB → 6,7MB** (2,75x), a belső-utazás look screenshottal igazoltan változatlan. A quantization three-natív (nincs extra decoder). Az eredeti a git-történetben marad.
- **A "cső" lecserélve CHROMATIC JOURNEY-re (2026-07-24):** a régi procedurális hullámvasút-cső (`TubeRide.jsx`) helyett a nyuszi-hole 3. része most egy Sketchfab-modell **belsejében** utazik. A modell: `chromatic_journey.glb` (Sketchfab, **Tycho Magnetic Anomaly, CC-BY-4.0** — ⚠️ ATTRIBÚCIÓ KELL éles használatnál!), egy sokszínű torus/donut. **Új komponens: `ChromaJourney.jsx`** — a kamera a donut cső középvonalán halad körbe (a modell YZ-síkjában, X a szimmetria-tengely), a haladási irányba nézve, és **a haladási tengelye körül pörögve** (roll), így a szivárvány-körök örvényként zúgnak el mellette. A **donut áll** (a baked "ringás" animáció frame 0-ra fagyasztva) — csak a kamera mozog. `rabbitRef` hajtja a `RIDE_IN=0.16 → RIDE_OUT=0.92` ablakban (ugyanaz mint a csőnél, így a portál→journey crossfade időzítés változatlan a `RabbitHole.jsx`-ben), a végén (`rabbitRef 0.86→1`) feketébe fade → átadás a kertnek (mint a cső sötét kijárata). **Beégetett értékek** (a `?chroma` laborban élőben belőve): `rideR:0.39, rideLoops:1, ridePhase:0, rollTurns:2.1, mry:1.5708, mscale:6`. Élőben ellenőrizve a valódi scroll-folyamban (teljes fedés, bent-utazás, roll, scroll-haladás). A `TubeRide.jsx` már nincs importálva (a fájl megvan, de holt kód — később törölhető). **Tuning-labor: `?chroma`** (`ChromaLab.jsx`) — 3 mód: Inspect / Portál (átrepülés a lyukon) / Belső utazás; a Belső ugyanezt a beégetett look-ot nyitja. ⚠️ A GLB **18MB / ~370k tris** — élesre optimalizálni kell (gltf-transform + webp textúrák) betöltés-hitch és bundle miatt.
- **A Sketchfab-look reprodukciója = SÖTÉT environment + BLOOM (2026-07-25):** a 4 textúra mind **szivárvány-gradient/pötty** (nincs bennük márvány-minta — a "márványos" fal a geometriából + az img0 pöttyös alpha-maszkból jön). A fő felületek (`mat0` pöttyök img0, `mat2` gradient img2) **FÉMES PBR** (metallic 0.53–0.68) → environment-tükröződés nélkül FEKETÉN renderelnek; a szalagok (`mat1`) fekete alap + **emissive** img1 (full-bright szivárvány) → maguktól izzanak. Két összetevő kell a Sketchfab-look-hoz: (1) **self-contained `ChromaEnv`** (three `RoomEnvironment` + PMREM, hálózat nélkül, exportálva a `ChromaJourney`-ből) — de **ALACSONY `scene.environmentIntensity`-vel** (~0.3), különben a fényes stúdió-env kimossa a fémfelületet (a bright RoomEnvironment default → túlexponált, kimosott). (2) **Bloom** (`@react-three/postprocessing`, ugyanaz mint a hero SpinScene-ben) **MAGAS `luminanceThreshold`-dal** (~0.65) — így csak a fényes emissive neon izzik, a fémfal sötét marad → izzó neon-vonalak sötét gazdag falon. `toneMappingExposure` (~0.9) trimmeli az össz-fényerőt. **VÉGLEGES beégetett production értékek** (`ChromaJourney.jsx`, user jóváhagyta 2026-07-25): **env 0.2, exposure 0.92, bloom intensity 0.30, threshold 0.56, chromatic aberration 0.0058** — a `?chroma` lab "FÉNY / TEXTÚRA" szekcióban mind csúszkázható (a lab alapértékei is ezek). **Tanulság:** metál PBR GLB environment MAP nélkül fekete/lapos; a neon "izzás" mindig BLOOM; ha túl világos/kimosott, az env-intenzitás túl magas. (Egy korai bloom-kísérlet feketét adott — az a drei `Environment preset="night"` CDN-fetchje + túl alacsony 0.25-ös küszöb miatt volt, NEM a bloom maga; a hero óta bizonyítottan működik headlessben is.)
- **Az eredeti .blend megvizsgálva (2026-07-25) — megerősíti a receptet, de NEM jobb forrás:** a user letöltötte az eredeti `chromatic-journey.zip`-et (`source/thingy.blend` + 4 eredeti textúra külön mappában). Headless Blender 5.1-gyel megnyitva: **NINCS fény** a jelenetben, **sötét world (0.05 szürke)**, **Filmic** view transform, az anyagok **NEM fémesek** (metallic 0.0), a szalagok **EMISSION** (emis_str 1.0), a "Dots Stroke" fal **NEM világít** (emis_str 0.0 = a sötét márványos fal), az eredeti glow **Eevee Bloom** volt. **DE a .blend 5.1-ben flat SZÜRKÉN renderel** (lerendereltem: szürke pill-alak) — a material/textúra-node-ok eltűntek (2023-as fájl, stripped/verzió-migráció; a textúrákat kézzel kellene visszakötni, guess-work). Ezért a **Sketchfab GLB a jobb forrás** (helyesen beégetett emissive textúrákkal). **Következmény a webes renderre:** `enhanceChromaMaterials` (exportálva a `ChromaJourney`-ből, a lab is hívja) most a .blend receptjéhez igazít: `metalness=0` (kiüti a GLB export metál-artefaktját, így nincs kimosó tükröződés), a meglévő emissive map-eket megtartja → a szalagok maguktól izzanak (nem kell environment-tükröződés), a pöttyös fal nem-emisszív marad = sötét fal. Sötét env (~0.3, halvány kitöltés a falnak) + bloom (glow) + Filmic-szerű tone mapping. Nem kellett CLI/Codex, sem újraexport.
- **VHS chromatic aberration (2026-07-25):** a user észrevette, hogy az eredetin van egy VHS-szerű "blurös duplikáció" — ez **chromatic aberration** (RGB-csatorna szétcsúszás a neon szélein). Bekötve a composerbe a bloom mellé (`@react-three/postprocessing` `ChromaticAberration`, production offset `[0.0026,0.0026]`), a `?chroma` labban "VHS szétcsúszás" csúszka hangolja.

- **A választás-elemek visszakapcsolva (2026-07-18–19):** a `SHOW_CHOICE` flag két külön flagre bomlott: `SHOW_TEXT_HAND=true` (felirat + pill-kéz — vissza vannak kapcsolva, a kert kész), `SHOW_AURA=false` (a régi migrén-aura továbbra is parkolva, újratervezésre vár). A régi `MatrixCurtain` komponens **véglegesen törölve** (nem csak kikapcsolva) — a mátrix-motívum most az ég-glitch rendszerben (`GardenSky`/`Butterflies`) él tovább, egy külön réteg duplikálná.
- **Belépési sorrend javítva (2026-07-18):** korábban a FELIRAT úszott be előbb (0.12-0.42), a KÉZ csak utána (0.34-0.72) — fordítva a kívánttól. Most: kert/ég/pillangók azonnal jelen vannak (nincs saját fade-jük), a kéz érkezik előbb (0.16-0.6), a felirat csak azután (0.55-0.85).
- **Felirat mélység-takarási hiba javítva (2026-07-18):** minden kerti növény-anyag `alphaMode:BLEND` (lágy lombozat-élek) → Three.js ezeket a nem-mélységet-író átlátszó sorba teszi; a felirat is `depthWrite:false` volt, ezért két ilyen objektum egymáshoz képesti sorrendjét csak a rajzolási sorrend döntötte, nem a valódi Z-mélység — a fű/virágok sosem takarták el megbízhatóan a szöveget, bármennyire hátrébb tolva. Javítva: a felirat (éles szélű, nincs lágy alfa-átmenete) most `depthWrite:true` → valódi Z-puffer alapú takarás mindennel szemben. A szobor emiatt igényelt egy erősen a szöveg elé húzott Z-értéket (a saját, dupla forgatása miatt a `z` mező nem 1:1 arányban felel meg a látható mélységnek) — empirikusan behatárolva.
- **Felirat-árnyék (2026-07-18):** troika `outlineWidth/Offset/Blur/Color/Opacity` alapú drop-shadow a feliraton, a fényirányhoz igazítva (jobbra-lefelé esik, mint a bal-felülről jövő nap mellett várható) — `sshadow` cfg-mező + `?arrival` csúszka.
- **`GardenSky` hullám-effekt átdolgozva "echo"-vá, majd egyszerűsítve (2026-07-18):** első verzió (egy vékony táguló gyűrű) → user kérésére CodeUniverse-stílusú többsávos "echo" (széles Gauss-burok + rövidebb periódus) → user visszajelzés: ez túl nagy, geometrikus "bullseye" mintát adott → végleges verzió: a hullám kizárólag a KÓD-GLYPHEK fényességén keresztül fejeződik ki (a haladó front felfényesíti az éppen alatta lévő karaktereket), nincs külön rajzolt kör/gyűrű a nyugodt égen.
- **Kerti szélmozgás kiterjesztve MINDEN elemre + valódi hajlítás (2026-07-17–18):** a korábbi merev, tő-körüli pivot-forgatás helyett minden növény (a szoborén kívül) **per-vertex cantilever-hajlítással** hajlik (`onBeforeCompile` shader-injektálás a `GardenModel` már klónozott anyagába) — a szél ereje 0-tól a tő felett a csúcsig nő, valós ívként, nem merev döntésként. A szélirány (`WIND_DIR`) minden elemnél a saját lokális terébe van transzformálva, hogy egységesen olvasson. Kulcs-hibák útközben: (1) a szél iránya kezdetben szimmetrikus volt ("libikóka"-hatás) → egyirányú, unipoláris lengésre javítva; (2) a mélység-számítás `matrixWorld`-re támaszkodott, ami verseny­helyzetet okozott néhány elemnél (helyesen bólogattak, mások nem) → a magasság-tartományt most kizárólag a modell saját lokális hierarchiájából számoljuk, determinisztikusan.
- **Szobor előrébb hozva a felirat elé (2026-07-17-18):** végül Z=-1,3-ra (a saját dupla forgatása miatt szükséges nagy korrekció), majd a user kérésére vissza kisebb túlzásra, majd a végleges pozíció a felirat-mélység-javítás után lett belőve.
- **A megérkezés-tér („a kert") felépítése (2026-07-15–17) — LEZÁRVA, SHOW_CHOICE visszakapcsolva:** teljes kert-elrendezés (18 elem: fű/nád/virágok/rododendron/levendula/szobor), 3D modellek Blenderrel FBX→GLB konvertálva és optimalizálva, `?garden` KERT-LABOR szerkesztő (paletta/kijelölés/húzás/fény), fény-irány és szobor-textúra javítások, glitchelő ég + 2 pillangó ok-okozati kapcsolattal (`GardenSky.jsx`, `Butterflies.jsx`) — részletek az előző verzió git-történetében, a lényeg a fenti pontokban tovább finomodott.
- **Rabbit hole 5. rész — választás-képernyő fő elemei (2026-07-14):** mátrix-függöny (később törölve) + auralátás (parkolva) + kattintható kezek + mind-jungle bekötés + Suspense-watchdog. Beégetett cfg: hx1.65/hy0.05/hs1.8/rx27/ry-27.
- **Rabbit hole 4. rész — megérkezés alap (2026-07-14):** `ArrivalScene.jsx` + `.css`, `pillhand.glb`, Jelvion font, negyedik scroll-fázis, `?arrival` editor.
- **Rabbit hole 3. rész — a hullámvasút-cső (2026-07-14):** `TubeRide.jsx`, CatmullRom görbe + Frenet-frame kamera-rig, pszichedelikus BackSide shader, olvashatósági javítások.
- **Rabbit hole 1-2. rész + belépő koreográfia + `&` landolás (2026-07-12–14):** portál-editor, `RabbitHole.jsx`, `&` zuhanás/landolás időzítés és horgonyzás a 3D logóhoz, egybefüggő oldal (nincs vágás/áttűnés).
- **Hero-utazás (korábbról):** kártyafal-mátrix → dot-globe headline → kód-terminál → mátrix-eső → `&` kitör és zuhan → landol a logóban. `?hand` editor.

---

## 📋 Terv / következő lépések

1. **A user élőben teszteli a teljes láncot (`?arrival` → Vihar "Teszt indítás")** — vihar + kóddá válás + örvény + feketelyuk + (editorban navigáció nélkül a fekete vég). Hangolja a csúszkákat: Vihar 4 + Szingularitás 5 (`stHold`/`sgCode`/`sgBurst`/`sgCollapse`/`sgBlack`) + a "szekvencia előnézet" scrub. Utána: a JSON-t elmenti/másolja → beégetjük a `DEFAULT_CFG`-be.
2. ~~**Navigációs handoff**~~ — **KÉSZ (2026-07-25).** A piros pirula → vihar → szingularitás → `window.location` redirect a `RED_PILL_URL`-re. Élesben még végig kell tesztelni a valódi scroll-folyamban (eddig editor-teszttel igazolva).
3. **Chromatic Journey:** (a) ~~optimalizálás~~ **KÉSZ (18,4MB → 6,7MB, look változatlan).** (b) ~~CC-BY-4.0 attribúció~~ **KÉSZ — az `#agency` footerében.** (c) opcionális finomítás: motion blur / portál→journey belépő simítása.
4. ~~**`#agency` szekció**~~ **MEGÉPÍTVE (2026-07-26)** — a kék pirula stúdió-oldala (`Agency.jsx`). MÉG HÁTRA: (a) a „code crystallizes into order" belépő-átmenet (opcionális signature); (b) a kapcsolati űrlap `FORM_ENDPOINT`-ja (Formspree/Resend — Alex adja); (c) valódi esettanulmány-assetek/thumbnailek a Kiválasztott munkákhoz.
5. **Aura újratervezése** (`SHOW_AURA` jelenleg kikapcsolva) — meg kell beszélni, mi legyen vele.
5. **Nyelvváltó (HU/EN):** stringek megvannak a content.js-ben, switcher hiányzik.
6. **Fontok bekötése:** a user letöltött Heathergreen + Moonscape fontok a `public/assets/fonts/`-ban, még nincsenek a felirat-editor FONTS listájában.
7. **Bundle-splitting** (~1.5MB unsplit, deferred).
8. **A FŐ Web & Style oldal git init + Vercel-deploy** — eddig csak a mind-jungle (creative-cortex) van kint; ez a projekt maga még nincs éles URL-en.

---

## 🅿️ Elparkolva későbbre

- **Kéz ujj-riggelés (2026-07-13):** egy rendesen riggelt asset (`first-person-hands-rigged.zip`) a folytatás útja, egy `/rig` labor-oldal épült hozzá (kivéve a projektből). Részletek a projekt-memóriában.
- **Migrén-aura (`AuraVision`) újratervezése** — jelenleg kikapcsolva, működik a kód, csak nem illik még jól a jelenethez.

## ⚠️ Ismert / halasztott

- Bundle nincs kódra bontva (build figyelmeztet, működik).
- `dist/` build után törölhető (user konvenció).
- A Codex-plugin háttér-feladatai (`codex:codex-rescue` → task) nem érik el a helyi gépet ezen a Windows-környezeten (ld. fent) — közvetlen munkával kerülendő, amíg meg nem oldódik.

---

## 🔧 Kulcs-fájlok

- `src/components/ArrivalScene.jsx` + `.css` — a megérkezés-jelenet: kert-háttér, felirat (3D `<Text>`, árnyékkal), pill-kéz, `Stage`/`PillHand`/`Title3D` komponensek, `DEFAULT_CFG` (2026-07-23: a nyuszi-mezők KIVÉVE, helyette a VIHAR mezői: `stDur`/`stWave`/`stShake`/`stLight`), `?arrival` editor "Vihar" szekcióval (4 csúszka + Teszt indítás/Visszaállítás gomb), `loadCfg`/`CFG_KEY`. A `stormRef = { triggered, startedAt }` a fő komponensben él, a `PillHand` piros-pirula kattintása billenti (a `StormShake`/`GardenSky`/`Butterflies` olvassa). `RED_PILL_URL` = webandstyle.com, jelenleg nincs meghívva sehol (a handoff még nincs bekötve, ld. fent).
- `src/components/GardenSky.jsx` — glitchelő ég shader + A VIHAR (2026-07-23) + a SZINGULARITÁS-DRAIN (2026-07-25: `uDrain` — a kód UV-warpja a központi lefolyó felé, örvényleve beszívva; `vScreen` dpr-független uv a pontos középpontért). A VIHAR (2026-07-23). Nyugodt állapot: kék→meleg gradient, felhők, szivárvány, pillangó-becsapódás (`uHitAmp` lokális glitch). Vihar: `uStorm` (sötét lila ég + felhő-sötétedés + tear/posterize/slip, az árva `uMegaAmp`-ot `max(uMegaAmp,uStorm)`-mal újrahasznosítva), `lightningFlash()` (sárga villám + bolt, `uLightning`), `uStormWave` (a kód-élesedés táguló frontja a képernyő közepéből — a `column`/`alive` küszöbök lejjebb tolva a front mögött). A GardenSky useFrame bélyegzi a `stormRef.startedAt`-et (közös időalap a StormShake-kel és a pillangókkal).
- `src/components/StormShake.jsx` — a földrengés (2026-07-23): a FIX arrival-kamerát rázza HELYBEN, főleg rotációs jitterrel (soha nem dolly — az felfedné a lapos kert-síkot) + kezdeti lökés, `stShake` szabályozza, reduceMotion alatt kikapcsol. A `stormRef`-et olvassa.
- `src/components/Singularity.jsx` — a vihar UTÁNI payoff (2026-07-25): `singularityState(stAge, cfg)` közös óra (code/burst/reveal/black/done fázisok); `SingularityVortex` (fullscreen spirál-örvény + feketelyuk shader, `onDone`→redirect); `CODE_DISSOLVE_PARS/FRAG` + `patchCodeDissolve` (a per-objektum lilás kóddá-válás+szétporladás, amit a `PillHand`/`GardenModel`/`GardenBg`/`Title3D` használ). A `stormRef.startedAt`-hez horgonyoz, `?arrival` "Szingularitás" csúszkák hajtják.
- `src/components/GardenItems.jsx` — kerti 3D modellek, `?garden` KERT-LABOR, per-vertex szél-hajlítás shaderrel (`onBeforeCompile`), `DEFAULT_ITEMS`/`DEFAULT_LIGHT`.
- `src/components/Butterflies.jsx` — 2 pillangó, `hitRef` írás a fal-kontaktnál (nyugodt glitch). Vihar-jelre (`stormRef`) `flee` 0→1 ~1mp alatt: felfelé + a legközelebbi képernyőszél felé menekülnek, pánik-jitterrel, és nem csapkodják tovább a falat. **A `?arrival` "szekvencia előnézet" (`sgPreview`) alatt `flee=1` azonnal** — a preview poszt-vihar állapot, ott a pillangók már rég elrepültek (különben úgy néznének ki, mintha nem menekültek volna a vihar elől).
- `src/components/RabbitHole.jsx` — portál-dive + CHROMATIC JOURNEY (2026-07-24: a `TubeRide` helyett `ChromaJourney`-t mountol/crossfade-el), `?rabbit` editor (FIGYELEM: ez a `?rabbit` query param a PORTÁL laborja — a nyuszi-FIGURA és a `?bunny` labor 2026-07-23-án TÖRÖLVE).
- `src/components/ChromaJourney.jsx` — a nyuszi-hole 3. része (2026-07-24): a `chromatic_journey.glb` donut BELSEJÉBEN utazó kamera (torus cső-középvonal + roll), `rabbitRef`-fel vezérelve, a végén feketébe fade. Beégetett `BAKED` értékek. `?chroma` labor (`ChromaLab.jsx`) tunolja ugyanezt. CC-BY-4.0 asset (attribúció kell).
- `src/components/ChromaLab.jsx` — `?chroma`: a Chromatic Journey tuning-labora (Inspect / Portál / Belső utazás módok), izolált, a journey-t nem érinti.
- ~~`src/components/TubeRide.jsx`~~ — a RÉGI hullámvasút-cső, **TÖRÖLVE a 2026-07-25 takarításkor** (a `ChromaJourney` váltotta le).
- `src/components/HandScene.jsx` — a hero-színpad, `?hand` editor.
- `src/experience/CodeUniverse.jsx` — mátrix-eső, `&` zuhanás/landolás.
- `src/App.jsx` — top-level routing: a teljes scroll-journey (`Experience`+`ChooseSection`+`RabbitHole`+`ArrivalScene`) + a `#agency` hash-routolt átvevő-oldal (`Agency`, kék pirula). A `?bunny` route TÖRÖLVE (2026-07-23).
- `src/components/DnaCaseStudies.jsx` + `.css` — a "Kiválasztott munkák" 3D DNS-ként (2026-07-26): irizáló kettős hélix (R3F canvas, `dna.glb`), amiből 4 case study kártya ágazik ki leader-line-okkal (a vonalak DNS-vége 3D-anchorra köt, minden frame-ben screenre projektálva → követi a szálat). Az anyag `MeshPhysicalMaterial` **iridescence**-szel (a nyers albedo fakó szürkés-kék volt; a szivárvány az anyagból jön) + brand-lila emissive-lift + env + bloom. 4 case study: Vanity Mirror / House on the Cliff / The Story of the Olive / Web & Style immersive. Mobilon: DNS felül, kártyák alatta listában (vonalak elrejtve). **Leader-line fix (2026-07-26):** az anchorok a hélix KÖZÉPTENGELYÉN ülnek (x=0,y=0, z-mentén elosztva) → forgás-invariáns, sose csúsznak le a szálról; a folyamatos `spin` KIVÉVE, csak bounded sway maradt (nem driftel). **Device-mockupok:** minden kártya kaphat `desktop`/`mobile` képet (a `work` itemben) → asztali böngésző-frame + telefon-frame a kártya alatt (`DeviceMockups`, `href`-fel linkelhető). Képek: `public/assets/work/` — a Vanity KÉSZ (`vanity-desktop.jpg` a Szolgáltatások-oldal, `vanity-mobile.jpg` a vanity hero; a user hozta, ffmpeggel optimalizálva). **`?dna` LEADER-LINE EDITOR (2026-07-26):** a `?dna` query-param bekapcsolja — a 4 számozott pontot rá lehet HÚZNI a DNS-re (raycast a felületre → a pont a helix LOKÁLIS terébe kerül, így 3D-ben, forgás közben is a szálon marad). A helix áll editorban. Panel: a 4 anchor értéke + Mentés (localStorage `ws-dna-anchors`) / Másolás (JSON) / Reset. Editorban a kártyák `pointer-events:none` (minden pont fogható), a pontok a kártyák fölé rajzolódnak. Production: `loadAnchors()` a localStorage-ból vagy a `DEFAULT_ANCHORS`-ból. **Az anchorok BEÉGETVE (2026-07-26, user által editorban belőve).** A mockup: desktop-frame + telefon-frame EGYMÁS MELLETT (bottom-aligned, nincs átfedés); a desktop `desktopVideo` esetén loopoló muted videót játszik (a Vanity-nál `vanity-desktop.mp4`, 10,4MB→1,17MB ffmpeggel), különben a `desktop` képet. A bal-oszlop kártyái (Vanity top:0 / Olive bottom:0) szét vannak húzva, hogy a magas mockup-kártya ne érjen a lentibe. Asset: `dna.glb` (11,5MB→4,7MB, gltf-transform webp+quantize; a nyers `.dae` Blender 5.1-ben nem importálható, a user hozott GLB-t).
- `src/components/Agency.jsx` + `.css` — a KÉK PIRULA stúdió-oldala (2026-07-26): csillapodott, prémium `#agency` átvevő-oldal (masthead / szolgáltatás-index / folyamat / **munkák = 3D DNS** `DnaCaseStudies` / Alexről / kapcsolat-űrlap). Tartalom: `content.js` `AGENCY` blokk (`useT()`). Kapcsolat: `FORM_ENDPOINT` (üres → `mailto` fallback) + `alex@webandstyle.com`. Footer: CC-BY attribúció. `onClose` törli a `#agency` hasht.
- `src/data/content.js` — az `AGENCY` blokk (HU+EN) a kék-pirula oldal minden szövegével.
